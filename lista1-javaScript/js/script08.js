function area(){
    var base=parseFloat(prompt("digite a base do triangulo"));
    var altura=parseFloat(prompt("digite a base do triangulo"));
    var area= base*altura/2;
    alert("O resultado é:"+area);
}